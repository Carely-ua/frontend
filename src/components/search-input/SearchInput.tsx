import InputMUI from '@mui/base/Input';
import styles from './SearchInput.module.scss';

export const SearchInput = () => {
  return (
    <div>
      <InputMUI className={styles.input} />
    </div>
  );
};
