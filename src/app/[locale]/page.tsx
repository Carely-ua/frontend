import { MainBanner } from '@/components';
import { NavigationSections } from './home-components';

const Home = async () => {
  return (
    <>
      <MainBanner />
      <NavigationSections />
    </>
  );
};

export default Home;
