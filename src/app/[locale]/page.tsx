import { Typography } from '@/ui-kit';
import { NavigationSections } from './home-components';

const Home = async () => {
  return (
    <>
      <NavigationSections />
    </>
  );
};

export default Home;
