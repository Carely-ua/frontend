query GetCart {
  cart {
    cartSum
    cartDiscountSum
    cartLength
    cartItems {
      id
      service {
        id
        name
        serviceType
        price
        discountPrice
        clinic {
          mainImage
          name
        }
      }
    }
  }
}