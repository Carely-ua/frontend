query GetCart {
  cart {
    cartLength
    cartItems {
      id
      doctor {
        id
        name
        image
      }
      service {
        id
        name
        serviceType
        price
        discountPrice
        clinic {
          mainImage
          name
          clinicType
          id
        }
      }
    }
  }
}