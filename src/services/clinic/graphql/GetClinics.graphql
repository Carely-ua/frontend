query GetClinics($filters: ClinicFilterInputType!) {
  getClinics(filters: $filters) {
    id
    name
    rating
    address
    image
    clinicType
    mapCoordinates {
      lat
      lng
    }
    reviewsCount
    specializations {
      id
      title
    }
    workingTime
  }
}