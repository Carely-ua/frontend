query GetClinic($filters: ClinicFilterInputType!) {
  clinic(filters: $filters) {
    id
    name
    rating
    address
    image
    clinicType
    workingTime
    reviewsCount
    mapCoordinates {
      lat
      lng
    }
  }
}